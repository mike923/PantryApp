{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./backend/bin/www",
    "heroku-postbuild": "cd backend && npm install"
  },
  "dependencies": {
    "react-native-dotenv": "^0.2.0"
  }
}
